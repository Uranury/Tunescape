version: "2"

linters:
  enable:
    - errcheck
    - staticcheck
    - govet
    - unused
    - misspell

linters-settings: {}

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck